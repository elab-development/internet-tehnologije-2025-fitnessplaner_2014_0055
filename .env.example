# Shared MySQL password (used by both the db and backend services)
DB_PASSWORD=changeme
DB_NAME=fitness_planer
# REQUIRED: long random string
JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRES_IN=1d
